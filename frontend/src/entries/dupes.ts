// dupes.html: the duplicate review. Read-only about the FILES -- nothing
// here deletes anything -- and writable about the GROUPING, which is a
// guess the page can now disagree with.
import "../dupes";
// the compare tray: kept across every surface, until dismissed
import "../compare-mount";
// a thumbnail that will not load says what it is instead of breaking
import "../pictures-mount";
