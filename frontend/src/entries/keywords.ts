// keywords.html: the keyword shelf. The only surface that EDITS the
// vocabulary -- renaming folds two words into one, forgetting takes a
// word off every picture.
import "../keywords";
// a thumbnail that will not load says what it is instead of breaking
import "../pictures-mount";
