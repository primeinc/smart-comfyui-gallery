// people.html: the People index. The person drawer's shell arrives as
// people's own import; whether it wires is a fact about this page's DOM.
import "../people";
// the compare tray: kept across every surface, until dismissed
import "../compare-mount";
