// person.html: one person's full profile. No overlay -- the profile IS the
// address the drawer stands in for -- so people.js finds no shell to adapt
// and wires only the rename.
import "../media";
import "../people";
// the compare tray: kept across every surface, until dismissed
import "../compare-mount";
