// Installed on every surface that shows media, the same way the compare
// tray is: "a picture that fails says what it is" is a property of the
// application, not of one page, and a rule nobody would remember to
// apply nine times.
import { mountPictures } from "./pictures";

mountPictures();
